-if @feed_items and @feed_items.any?
  %ol.microposts
    =render :partial => 'shared/feed_item', :collection => @feed_items
  =will_paginate @feed_items
-else
  %span.timestamp= "No new post by #{current_user.name}"